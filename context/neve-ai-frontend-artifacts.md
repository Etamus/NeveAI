# Neve AI - Frontend Artifacts

- Current static frontend served by backend lives at `backend/neveai/frontend`; after `npm run build`, sync `build/*` there for local app packaging.
- Legacy memory may mention `backend/open_webui/frontend`; verify current package path before copying artifacts.
